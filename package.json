{
  "name": "planix",
  "version": "0.1.0",
  "private": true,
  "description": "WBS Tool \u2014 Planning & Control cho PM th\u1ecb tr\u01b0\u1eddng Nh\u1eadt. Xem SPEC.md.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "packages/core",
    "packages/server",
    "packages/web"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:golden": "vitest run --project core golden",
    "lint": "eslint . && prettier --check .",
    "format": "prettier --write .",
    "typecheck": "tsc --build && tsc -p packages/core/tsconfig.json && tsc -p packages/server/tsconfig.json && tsc -p packages/web/tsconfig.json",
    "dev:web": "npm run dev --workspace @planix/web",
    "seed:dev": "npm run build --workspace @planix/core && npm run build --workspace @planix/server && node packages/server/dist/dev-seed.js data/dev.db",
    "dev:server": "PLANIX_DB=data/dev.db node packages/server/dist/index.js",
    "smoke": "bash scripts/smoke.sh"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^22.20.2",
    "@vitest/coverage-v8": "^5.0.0",
    "eslint": "^10.10.0",
    "prettier": "^3.9.6",
    "typescript": "~6.0.3",
    "typescript-eslint": "^8.70.0",
    "vitest": "^5.0.0"
  }
}
